from tkinter import *

root = Tk()
root.geometry("500x500")
root.title("calculator")
root.config(bg="gray")
root.resizable(0, 0)

l = Label(root, text="hello", fg="green", bg="gray").pack()

root.mainloop()
